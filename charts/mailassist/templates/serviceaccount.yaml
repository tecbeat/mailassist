{{/*
ServiceAccount is managed as a Helm hook in hook-prereqs.yaml (weight -10)
to avoid duplication conflicts on re-install after --atomic rollback.
See: https://git.teccave.de/tecbeat/mailassist/-/issues/107
*/}}
