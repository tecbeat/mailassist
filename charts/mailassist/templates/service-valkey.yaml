{{- if .Values.valkey.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailassist.valkey.fullname" . }}
  labels:
    {{- include "mailassist.labels" . | nindent 4 }}
    app.kubernetes.io/component: valkey
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: valkey
      protocol: TCP
      name: valkey
  selector:
    {{- include "mailassist.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: valkey
{{- end }}
