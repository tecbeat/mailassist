{{- if .Values.valkey.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "mailassist.valkey.fullname" . }}
  labels:
    {{- include "mailassist.labels" . | nindent 4 }}
    app.kubernetes.io/component: valkey
spec:
  replicas: 1
  serviceName: {{ include "mailassist.valkey.fullname" . }}
  selector:
    matchLabels:
      {{- include "mailassist.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: valkey
  template:
    metadata:
      labels:
        {{- include "mailassist.labels" . | nindent 8 }}
        app.kubernetes.io/component: valkey
    spec:
      containers:
        - name: valkey
          image: "{{ .Values.valkey.image.repository }}:{{ .Values.valkey.image.tag }}"
          imagePullPolicy: {{ .Values.valkey.image.pullPolicy }}
          command:
            - valkey-server
          args:
            - "--requirepass"
            - "$(VALKEY_PASSWORD)"
            - "--maxmemory"
            - {{ .Values.valkey.config.maxmemory | quote }}
            - "--maxmemory-policy"
            - {{ .Values.valkey.config.maxmemoryPolicy | quote }}
            - "--appendonly"
            - {{ .Values.valkey.config.appendonly | quote }}
          ports:
            - name: valkey
              containerPort: 6379
              protocol: TCP
          env:
            - name: VALKEY_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "mailassist.secretName" . }}
                  key: VALKEY_PASSWORD
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - "valkey-cli -a $VALKEY_PASSWORD ping"
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          resources:
            {{- toYaml .Values.valkey.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        {{- if .Values.valkey.persistence.storageClass }}
        storageClassName: {{ .Values.valkey.persistence.storageClass | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.valkey.persistence.size }}
{{- end }}
